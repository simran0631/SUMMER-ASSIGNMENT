#include<stdio.h>
//Recursive function to reverse a number
int reverse(int n)
{
    if(n==0)
    return rev;
    rev=rev*10+(n%10);
    return reverse(n/10);
}
int main()
{
    int n;
    printf("Enter a number:");
    scanf("%d",&n);
    printf("Reversed number=%d\n",reverse(n));
    return 0;
}